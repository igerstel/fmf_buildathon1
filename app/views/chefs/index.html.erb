<h1>Listing chefs</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Address</th>
    <th>Order Amt</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @chefs.each do |chef| %>
  <tr>
    <td><%= chef.name %></td>
    <td><%= chef.address %></td>
    <td>$<%= chef.payment %></td>
    <td><%= link_to 'Show', chef %></td>
    <td><%= link_to 'Edit', edit_chef_path(chef) %></td>
    <td><%= link_to 'Destroy', chef, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Chef', new_chef_path %> |
<%= link_to 'Show Produce', produces_path %> |
<%= link_to 'Show Farmers', farmers_path %>